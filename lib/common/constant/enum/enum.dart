enum ViewState { idle, busy, empty, info, error }

enum Env { dev, prod }

enum NetworkType { wifi, mobile, bluetooth }

enum Knowledge {
  skills,
  softwareUsed,
}

enum Service { mobileApp, iot }

enum ProjCategory {
  featured,
  recent,
  all,
  mobileApp,
  electronic,
}

enum Layout {
  small,
  medium,
  large,
}
